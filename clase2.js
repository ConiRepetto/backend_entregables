let valoresOriginles


//map

let arrayTest = [1, 2, 3, 4, 5]
const functionMap = (array, callback) => {
    let newArray = [];
    for (let index = 0; index < array.length; index++) {
        newArray.push(newValue)
    }
}

